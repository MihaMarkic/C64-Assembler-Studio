# Binaries directory

Compiled files here are meant as a temporary solution to have fresher ones that they are on NuGet repository.
Currently only AvaloniaEdit binaries are here.

Eventually app will revert back to using NuGet.